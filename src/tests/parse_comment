# This is a single-line comment

server {
    port 80; # This is an inline comment
    
    # You can use comments to explain configuration blocks
    location / {
        root /var/www/html; # Document root directory
        name "foo";
        var 'single';
    }
}